// Fill these from your Supabase project → Settings → API.
// The anon key is SAFE to expose publicly — Row-Level Security protects data.
// Until both are set, the app runs fully; social features stay hidden.
// See SUPABASE_SETUP.md for the 5-minute setup.
export const SUPABASE_URL = "";       // e.g. "https://abcd1234.supabase.co"
export const SUPABASE_ANON_KEY = "";  // the long "anon public" key
